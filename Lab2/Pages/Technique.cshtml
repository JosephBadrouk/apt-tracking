@page
@model TechniqueModel
@{
    ViewData["Title"] = TechniqueModel.Name;
}

        <div class="w3-container">
            <div class="w3-display-container" style="height: 70px;">
                <div class="w3-display-topmiddle">
                    <h2 class="display-4 text-center w3-center" id="techniqueName">@TechniqueModel.Name</h2>
                </div>
                <div class="w3-display-topright">
                    <form method="post">
                        <button name="btnTrack" asp-page-handler="Track" class="w3-button @TechniqueModel.ButtonColor w3-right">
                            @ViewData["TrackStatus"]
                        </button>
                    </form>
                </div> 
            </div>
        </div>
<hr />
<div class="w3-container">
    <table class="w3-table-all">
        <tr>
            <th>Tactic Used</th>
        </tr>
        <tr>
            <td>@TechniqueModel.Tactics.Aggregate((i, j) => i + ", " + j)</td>
        </tr>
    </table>
    <table class="w3-table-all">
        <tr>
            <th>Description</th>
        </tr>
        <tr>
            <td>@Html.Raw(System.Web.HttpUtility.HtmlDecode(@TechniqueModel.Description))</td>
        </tr>
    </table>

</div>
